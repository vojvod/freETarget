import React, {Component} from 'react';
import {
    Container, } from 'reactstrap';
import '../css/main.css';

class Rifle extends Component {
    render() {
        return (
            <Container style={{maxWidth: '100%'}}>
                rifle
            </Container>
        );
    }
}

export default Rifle;
